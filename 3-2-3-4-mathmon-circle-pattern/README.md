# 매스몬 원 무늬 디자이너 ★ 단원 정점

에듀잇티 수학 게임 시리즈 3단원(원) 4차시이자 단원 정점입니다.

- 대상: 3학년 2학기 3단원 4차시
- 학습: 원으로 규칙과 무늬 만들기 (같은 크기 원을 규칙대로 배치)
- 문제: 일부 놓인 무늬에서 다음에 놓을 같은 반지름 원의 자리를 고르는 문제 10개
- 방식: 놓인 원 + 후보 자리(가·나·다·라)를 보고 규칙에 맞는 자리를 고르는 1단계 선택형
- 보상: 한 문제마다 랜덤 무늬 점수 이벤트(증가/감소/0/완벽한 무늬/무지개)
- 결과: `점무늬 → 작은 무늬 → 무늬 → 큰 무늬 → 대디자인`(무지개면 `무지개 무늬`)
- 실행: `index.html`을 브라우저에서 열기

## 설계 의도

`매스몬 원 무늬 디자이너`는 단원 정점으로, 앞 차시에서 익힌 **중심·반지름·지름(1차시)**, **컴퍼스로 같은 반지름 원 그리기(2차시)**, **지름=반지름×2(3차시)**를 "같은 크기 원을 규칙대로 잇기"로 모읍니다. 후보에는 항상 핵심 오개념을 넣습니다 — **간격이 어긋난 자리**, **줄에서 벗어난 자리**, **크기가 깨진 자리**. 학생은 같은 줄·같은 간격·같은 크기라는 규칙을 직접 보고 다음 자리를 고릅니다.

보상·등급 구조는 시리즈 공통 엔진을 그대로 쓰고 무늬 테마로 이름만 바꿨습니다. 문제 화면 도형은 생성 이미지가 아니라 **SVG**로 그립니다.

## 자산 상태 (TODO)

- 커버 배경·제목 타이틀 아트, 결과 등급 6장, `circle-pack` 매스몬 동행은 이미지 생성 도구 보유 세션에서 만든 뒤 연결합니다. 연결 전까지 첫 화면은 임시 텍스트 제목, 결과는 등급 색 카드로 동작합니다. 이후 첫 화면을 `generated-title-overlay` 표준으로 승격합니다.
